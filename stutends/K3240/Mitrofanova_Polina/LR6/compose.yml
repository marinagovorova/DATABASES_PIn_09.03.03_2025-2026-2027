services:
  mongodb:
    image: mongo:latest
    container_name: biblio_db_mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: polina_mitrofanova
      MONGO_INITDB_ROOT_PASSWORD: pstg1234
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: